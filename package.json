{
  "name": "pdf-renamer-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron . --inspect",
    "pack": "electron-packager . --overwrite --platform=win32 --arch=x64 --prune=true --out=release-builds"
  },
  "dependencies": {
    "electron": "^28.0.0",
    "pdfjs-dist": "^5.4.54",
    "sweetalert2": "^11.22.5"
  },
  "devDependencies": {
    "electron-packager": "^17.1.2"
  }
}
